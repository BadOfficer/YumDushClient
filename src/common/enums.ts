export enum Role {
	ADMIN,
	CURIER,
	CUSTOMER,
}

export enum OrderStatus {
	ACTIVE = 'active',
	CONFIRMED = 'confirmed',
	DELIVERED = 'delivered',
}

export enum DropDownType {
	LEFT = 'left',
	RIGHT = 'right',
}
